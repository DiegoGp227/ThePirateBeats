services:
  backend:
    build:
      context: ./back
    container_name: pirate-beats-backend
    ports:
      - "8000:8000"
    volumes:
      - downloads:/app/downloads
    restart: unless-stopped

volumes:
  downloads: